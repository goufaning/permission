spring:
  datasource:
    url: jdbc:mysql://123.206.82.234:3306/permission?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
  aop:
    proxy-target-class:
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
      # 这个配置会将执行的sql打印出来，在开发或测试的时候可以用
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
my:
  shiro:
      # 后端免认证接口 url
      anonUrl: /login
      # token有效期，单位秒
      jwtTimeOut: 3600

server:
  port: 8080
