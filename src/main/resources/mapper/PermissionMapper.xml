<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.goufn.permission.mapper.PermissionMapper">
    <!--低调小熊猫专用-->
    <resultMap id="resourceMap" type="com.goufn.permission.entity.Permission">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="url" column="url"/>
        <result property="permission" column="permission"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="createtime" column="create_time"/>
        <result property="updatetime" column="update_time"/>
    </resultMap>
    <select id="findPermsByUserId" parameterType="java.lang.Integer" resultType="java.lang.String">
        select
        DISTINCT p.permission
        from permission p
        LEFT JOIN role_permission rp ON p.id=rp.permissionId
        LEFT JOIN user_role ur on rp.roleId = ur.roleId
        WHERE ur.userId=#{userId} and p.permission is not null and p.permission!='' and p.status=1
    </select>
</mapper>